# NeuroLab enrichment pipeline (Phase 0)
# Python 3.9+ recommended. Install: pip install -r requirements-enrichment.txt
# Pinned to versions verified with verify_environment.py (Phase 0 + 1).

# Core: cognitive decoding + parcellation (latest tested)
neuroquery==1.1.0
nilearn==0.13.1
nibabel==5.3.3
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# Optional (Phase 4 biological layer)
neuromaps>=0.0.4

# Optional (graph_distance_correlation.py --use-ic: Lin/Resnik/JC with Sanchez intrinsic IC)
nxontology>=0.5.0

# NeuroSynth cognitive decoder (NiMARE)
nimare>=0.0.10

# Required: structural (ENIGMA) and gene (abagen) for rebuild_all_caches.py
# enigmatoolbox (not on PyPI): pip install git+https://github.com/MICA-MNI/ENIGMA.git
abagen>=0.2

# Optional: compound/nootropic vocabulary expansion
requests>=2.25.0

# Optional: ChEMBL supplementary binding cache (CC-BY-SA 3.0)
# chembl-webresource-client>=0.10.0

# Optional: KG-to-brain GNN (build_heterogeneous_graph.py, train_kg_to_brain_gnn.py).
# Install matching torch wheel index for your CUDA version, e.g.:
#   pip install torch_geometric>=2.5.0
# torch_geometric>=2.5.0

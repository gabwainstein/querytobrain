# Legacy Docs

This `docs/` folder is intentionally minimal in the personal export.

## Status

- Legacy integration and org-specific archive files were removed from this export.
- Current guidance lives under `neurolab/docs/` and the top-level `README.md`.
- References to earlier org-specific product framing should be treated as historical context only.

## Use

Current guidance lives in `neurolab/docs/` and the top-level `README.md`.
